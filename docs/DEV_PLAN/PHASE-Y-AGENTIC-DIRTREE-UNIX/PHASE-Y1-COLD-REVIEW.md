# PHASE-Y1-COLD-REVIEW.md

Two independent passes, 2026-09-20.

## Pass 1 — verdict "amend first"

Facts confirmed (script's only consumer is the skill; script derives its root from its own path;
no move happened). Judgment challenged: promoting `public-docs-i18n` was "defensible but
borderline… the decision overclaims" — the coupling is one-way, no new evidence had arrived since
Y0 called the skill borderline, promotion needs a code edit and empties `agentic/skills/`, and the
option "pack that only indexes the script" had not been weighed. Negative-control claims were
partly confirmed (`ide-mcp/` "Not yet created" is real; `agents/` was inaccurate rather than
ambiguous; the "deliberately absent" wording was the weakest point). Also: stale planning files
and no saved transcripts.

Five demands: save transcripts · correct `agents/` wording · record rejected alternatives and the
reversal · add the missing Y2 hazards · make INDEX/cascade/Y2 consistent. All applied; the row
was **reversed** to keep-leaf.

## Pass 2 (inside the Y2 review) — verdict: promote Y1 to DONE

All five demands verified done; the reversal judged sound ("does not swing to a new overclaim
beyond F1"). Residual nits, fixed: the decision's evidence heading claimed more than the control
shows (now "the legend, not the layout, was the defect", plus what the evidence does *not* show);
the Y1 report mis-located the Amendment section; INDEX still said "empty if all promoted".
