[project]
name = "ttod"
version = "3.0.0"
description = "The Tao of Development — pedagogical wisdom database for developers walking the path"
license = {text = "MIT"}
authors = [{name = "Rubén Vega Balbás", email = "ruvebal@crea-comm.net"}]
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9.0",
    "pyyaml>=6.0",
    "jsonschema>=4.0",
]

[project.scripts]
ttod = "cli:app"
