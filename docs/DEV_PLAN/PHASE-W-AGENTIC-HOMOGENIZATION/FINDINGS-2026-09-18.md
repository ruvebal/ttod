# FINDINGS — 2026-09-18 (read-only inventory)

**Cascade:** Phase W — Agentic Homogenization  
**Mode:** evidence for planning; no files moved; no branch created  
**Checked by:** planning session against live stores on Tanit

## Commands run

```bash
find ~/src/ttod/{agentic,.cursor,.claude} -type f
find ~/src/.agents -type f
ls ~/src/.cursor/agents ~/src/.cursor/skills
git -C ~/src/ttod branch -a | grep -i agentic
wc -l ~/src/ttod/AGENTS.md ~/src/ttod/CLAUDE.md
```

## Observed layout (TTOD repo)

```
ttod/
  AGENTS.md                 ← tool-agnostic root contract (canonical)
  CLAUDE.md                 ← thin redirect → AGENTS.md
  agentic/
    report-steward/         ← pack: agent/ · rules/ · skills/ · scripts/ · PACK.md
  .cursor/
    rules/ttod-editing.mdc  ← full body (not yet a landing)
    skills/public-docs-i18n/SKILL.md  ← full body (not yet a landing)
  .claude/
    agents/cascade-phase-executor.md   ← already a stub → ~/src/.agents/…
    agents/cascade-cold-reviewer.md    ← already a stub → ~/src/.agents/…
```

## Observed layout (studio, outside TTOD git)

```
~/src/.agents/              ← PLAN + partial migration (README dated 2026-09-11)
  README.md
  agents/cascade-*.md       ← canonical bodies for cascade subagents
  skills/assignment-forger/ ← skeleton only
  rules/                    ← empty
~/src/.cursor/
  agents/*.md               ← stubs pointing at .agents
  skills/*                  ← still fat (≈18 skills) — out of Phase W scope except
                              as a dependency map for stubs that TTOD docs cite
```

## Drift / risk notes

| ID | Finding | Severity for Phase W |
| --- | --- | --- |
| F1 | Two “canonical” homes already exist (`ttod/agentic/` and `~/src/.agents/`) with no written map in `ttod/AGENTS.md`. | P0 for AG0 freeze |
| F2 | `.cursor/rules/ttod-editing.mdc` and `public-docs-i18n` still hold full bodies — contrary to the landing goal. | P1; AG2/AG3 |
| F3 | `.claude/agents/*` already redirect to `~/src/.agents/`, not to `ttod/agentic/`. Homogenizing TTOD without a studio map would *re*-point or leave a split brain. | P0 for AG0 |
| F4 | CI (`public-docs-pages.yml`) and `scripts/generate-ts5-teaching-baseline.sh` hardcode `agentic/report-steward/...` paths — keep those working; do not invent a second privacy watcher path. | P1; AG4 |
| F5 | Remote branch `origin/agentic/gh-pack` exists; Phase W must pick a **new** branch name to avoid colliding history. | P2; AG1 |
| F6 | Evergreen `AGENTS.md` still teaches `~/src/.cursor/skills/ttod-bridge/` as the bridge path — studio skill, not TTOD-local. Rewrites must distinguish *studio skill location* from *TTOD agentic tree*. | P1; AG4 |
| F7 | `AGENTS.md` § Integration names “DevIAC MCP” (export ingest) but not in-repo `services/mcp/` or the Astro/Oracle FastMCP client path — agents cannot discover the product MCP from the root contract. | P1; AG0 freeze + AG4 |
| F8 | No bidirectional link `AGENTS.md` ↔ `agentic/` yet (`agentic/` has pack READMEs only). | P1; AG2 README + AG4 |
| F9 | No project `.cursor/mcp.json`; students have no shared Astro/Svelte IDE MCP. Docker `services/mcp` is easy to confuse with IDE docs MCP. | P1; AG6 + AGENTIC-HARNESS.md |
| F10 | Phase W AG0–AG5 do not wire frontier-orchestrator + Ollama validator fleet; only `cascade-phase-executor`, `cascade-cold-reviewer`, and report-steward are ready validators. | Info; documented in AGENTIC-HARNESS.md §3; AG6 adds MCP existence probes only |

## Rejected hypotheses

- “Delete `.cursor` and `.claude` entirely” — rejected: tools will not discover work without landings.
- “Use only `~/src/.agents/` and empty `ttod/agentic/`” — rejected for this cascade’s stated goal: in-repo, visible, pedagogical `agentic/`; report-steward already lives there.
- “One file format for all tools” — rejected: honesty rule in `~/src/AGENTS.md`; frontmatter stays per landing.

## Safe next action

Product owner opens AG0 (inventory + naming freeze decision record). **Do not create the
branch** until AG0 is DONE and AG1’s Acceptance names the frozen branch string.
