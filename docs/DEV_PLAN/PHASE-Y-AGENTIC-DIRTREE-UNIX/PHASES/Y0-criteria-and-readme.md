# Y0 — Criteria freeze + README rewrite

**Gate before start:** product-owner authorization of Phase Y INDEX — **given 2026-09-20**.

## Do

1. Add `agentic/ide-mcp/PACK.md` (new file, mirrors `report-steward/PACK.md`: surface table +
   one line on the `mcp.cursor.json` ↔ `.cursor/mcp.json` byte-identical exception).
2. Rewrite `agentic/README.md` around the INDEX criteria table — one legend: pack / leaf rule /
   leaf skill / landing / cite-not-absorb, plus the live `find` table of what exists today.
   Mark `lao-tzu-tao-compose/` as "local, untracked (generated by the bibliography pipeline)".
3. Epigraphs (all required, all verified live, none paraphrased from memory):
   - **TTOD by ID:** `arch-001` (boundaries) **and** `arch-014` (template / garden) — re-read
     both texts from `ttod.yml` and quote them exactly.
   - **Cathedral / Bazaar:** one short quotation, with the Chicago author–date cite taken from
     the DevIAC guide `cathedral-bazaar-companion.md` (its `evaluator_safe=yes` entry) and
     re-verified by running `ahmes query --cite "$DB:$NODE" --style chicago-author-date` on the
     guide's anchor nodes. Metaphor and parallel only — hedged, never evidence for studio
     pedagogy. If the cite fails to reproduce, drop the epigraph and record why; do not
     hand-write a citation.
4. Harness honesty: orchestrator ≠ `~/src/deviac/agentic` (does not exist); use cascade-forge +
   `.agents`. Say this with **role labels**, not absolute paths.
5. Run `check_public_privacy.py` on every tracked file touched. The repo is public; the README
   currently fails at the `~/src/.agents/` mention introduced by Phase W's AG2 — fix it.

## Do not

- Move files (Y2's job, if any).
- Accept TTOD proposals.
- Commit `lao-tzu-tao-compose/` (untracked, watcher findings pending owner sanitization).
- Claim ESR as pedagogical proof.

## Exit — `PHASE-Y0-REPORT.md`

README diff summary · `ide-mcp/PACK.md` added · both TTOD quotes checked live · Cathedral cite
reproduced (or dropped with reason) · watcher output · **legibility test result** (cold reviewer,
README + `find` only).
