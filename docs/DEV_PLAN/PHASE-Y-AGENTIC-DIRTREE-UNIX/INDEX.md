<!--
PHASE-Y — Agentic dirtree Unix tidy (packs vs leaves) + Dear Tree collection.
Planning pack only until product-owner authorization. Author: orchestrator · 2026-09-20
-->

# Phase Y — Agentic dirtree Unix tidy (“Dear Tree”)

**Status:** AUTHORIZED 2026-09-20 · plan audited and amended (see § Plan audit) · Y0 DONE · Y1 decided (flat packs; `public-docs-i18n` stays a leaf). No moves yet, no `ttod.yml` mutation.
**Depends on:** Phase W COMPLETE (AG0–AG6 on `main`). Phase X optional (docs guides).
**Programme letter:** Y (Q–X taken).
**Author:** Rubén Vega Balbás PhD · cascade drafted 2026-09-20

## One-sentence claims

- **Product:** `ttod/agentic/` stops feeling like a tangled forest — packs and leaves are
  distinguishable by path, and the README is the legend a half-agentic Unix engineer can trust.
- **Engineering:** explicit placement criteria (below) drive a small, gated move set; CI paths
  for `report-steward` stay locked; landings stay thin.
- **Pedagogy:** after the tidy gate, `lao-tzu-tao-compose` is used to stage Dear Tree proposals
  (already drafted in ttod-bridge `pending/`) — human accept only.

## Live snapshot (2026-09-20)

| Fact | Value | Where checked |
| --- | --- | --- |
| Current tangle | Packs at top level (`report-steward`, `ide-mcp`, `lao-tzu-tao-compose`) + flat `rules/` + flat `skills/public-docs-i18n` + pack-internal `rules/`/`skills/` | `find agentic -maxdepth 3` |
| README stale vs W+AG6+compose pack | Lists contents but no placement criteria, no Cathedral/Bazaar epigraph, weak legend | `agentic/README.md` |
| `~/src/deviac/agentic` | **Does not exist** | `ls` — DevIAC still uses `.cursor/{rules,skills,agents}` |
| Studio orchestrator home | `~/src/.cursor/skills/cascade-forge` + `~/src/.agents/agents/` | studio README |
| Cathedral coat | Ahmes `…/eric_s_raymond_2001_cathedral_bazaar_musings_…_7792be4e` · vectors `project_slug=scholar-alchemy` · Athanor bibliography hit in `profield-digital-creativity` | live broadcast |
| Cite status Cathedral | **FIXED 2026-09-20** (DevIAC-canonical guides) — Rev. ed. O’Reilly 2001 · ISBN `9780596001315` · `evaluator_safe=yes` · see `deviac/docs/guides/cathedral-bazaar-companion.md` + `studio-harness-test.md` | Ahmes coat `…7792be4e` |
| Dear Tree proposals | 5 staged `origin: blackbox` under ttod-bridge `pending/` — verified 2026-09-20: exactly 5 files match `20260920T1656*` | `ls pending/` |
| Repo visibility | **PUBLIC** on GitHub — `agentic/` is public text, not a private tree | `gh repo view --json visibility` |
| Privacy watcher on `agentic/` | Not run by CI (CI scans `docs/public`), but `agentic/README.md` on `main` already fails it (line 20, a `~/src/…` path introduced by W's AG2); the untracked `lao-tzu-tao-compose/` pack has 10 more tilde-path findings | `check_public_privacy.py agentic/` |
| `PACK.md` coverage | `report-steward` ✔ · `lao-tzu-tao-compose` ✔ · **`ide-mcp` ✘** (W's AG6 omitted it) | `find agentic -name PACK.md` |
| `lao-tzu-tao-compose/` tracking | Untracked, generated by the bibliography pipeline; **not committed by Phase Y** until its watcher findings are sanitized (owner decision) | `git status` |

## How to read this pack

| Face | File |
| --- | --- |
| Index (this) | entry + criteria + naming discussion |
| Orchestrator | [`PHASE-Y-AGENTIC-DIRTREE-UNIX-CASCADE.md`](PHASE-Y-AGENTIC-DIRTREE-UNIX-CASCADE.md) |
| Phases | [`PHASES/`](PHASES/) Y0→Y4 |

## Placement criteria (Unix engineer · half agentic · half human)

Treat `agentic/` like `/usr/local` with a written FHS:

| Kind | Path shape | Criterion (all must hold) | Counter-example today |
| --- | --- | --- | --- |
| **Pack** | `agentic/<pack-name>/` with a `PACK.md` at its root (mandatory — a directory without one is not a pack) | ≥2 surfaces **living inside `agentic/`** that must move together (skill+rule, skill+script, agent+skill) **or** a CI/lock path **or** a multi-file harness. A skill that merely *calls* a standalone repo-level CLI stays a leaf | `report-steward`, `ide-mcp` (gets its `PACK.md` in Y0), `lao-tzu-tao-compose` (local, untracked) |
| **Leaf rule** | `agentic/rules/<name>.md` | Single always-on discipline; one Cursor landing; no companion scripts | `ttod-editing.md` |
| **Leaf skill** | `agentic/skills/<name>/SKILL.md` | Single named workflow; may call a standalone repo-level CLI (declare it in the README row); becomes a pack when companion code would live inside `agentic/` | `public-docs-i18n` (calls `scripts/translate_public_docs.py`) — decided leaf in Y1 |
| **Landing** | `.cursor/` / `.claude/` | Frontmatter + redirect only; never a second body (sole exception: `.cursor/mcp.json`, an identical copy because JSON cannot redirect) | already W policy |
| **Reserved** | `agentic/agents/` | Absent until a genuinely TTOD-only agent exists; the cascade subagents are studio-wide (W0 §2) | — |
| **Cite-not-absorb** | outside TTOD | Studio skills (`ttod-bridge`, `cascade-forge`) stay studio | W0 list |

**Unix maxims encoded:**

1. **Do one thing well** — a pack is one governed method; a leaf is one discipline.
2. **Names are destiny** — one star per sky (`arch` voice); no `rules/` at two depths without a legend.
3. **Pipes not copies** — landings are pipes; edit-home is the file.
4. **Worse is better** — prefer a boring taxonomy over clever nesting that agents misread.
5. **Release early / often** — ship the legend (README) before inventing a third layout style.

## Target shape (proposed — freeze in Y0)

```text
agentic/
  README.md                 ← legend + epigraphs + criteria table (live)
  rules/                    ← leaf rules only
  skills/                   ← leaf skills only (today: public-docs-i18n)
  report-steward/           ← pack (CI-locked path — do not rename without CI PR)
  ide-mcp/                  ← pack
  lao-tzu-tao-compose/      ← pack (compose lens; not a second rules namespace)
  # optional later:
  # packs/                  ← ONLY if Y0 chooses nesting; default is flat packs at agentic/*
```

**Default decision (recommended):** keep packs as **siblings** under `agentic/` (Unix: flat is fine
when the set is small). Do **not** introduce `agentic/packs/` unless the cold review proves
agents still confuse packs with `rules/`/`skills/`.

## Collection naming — “deer / dear / dirtree”

| Candidate id | Display name | Verdict |
| --- | --- | --- |
| `dear_tree` | The Tao of the Dear Tree | **Recommended** — affection for structure; pun readable aloud |
| `dirtree_wisdom` | Dirtree Wisdom | Clear, less ironic, more ops-manual |
| `deer_tree` | The Tao of the Deer Tree | Fun but opaque in print (typo risk) |
| `cathedral_bazaar_layout` | Cathedral & Bazaar of Layout | Overclaims ESR as TTOD evidence |

Irony (clear?): the *dear* tree is the directory you love enough to name carefully; the *deer*
tree is what you get when packs and leaves share a parent with no legend — Bambi lost in a
forest of similarly named folders. Cathedral = planned naming; bazaar = emergent packs
(`lao-tzu-tao-compose` arriving post-inject). Collection filters quotes about layout coherence,
naming homes, and non-forcing structure.

## Harness honesty (orchestrator)

This mini-cascade is **not** run from `~/src/deviac/agentic` — that path **does not exist**.
Master orchestration uses:

| Role | Home |
| --- | --- |
| Cascade shape | `~/src/.cursor/skills/cascade-forge/SKILL.md` |
| Cold / phase subagents | `~/src/.agents/agents/cascade-*.md` |
| Knowledge lanes | DevIAC vectors (`:8100`) + Athanor + Ahmes cite |
| Compose lens | `ttod/agentic/lao-tzu-tao-compose` |
| Propose | `~/src/.cursor/skills/ttod-bridge` |

Y4 may optionally add a **pointer decision** (not a rename) documenting DevIAC’s future
`agentic/` vs `.cursor` alignment with studio `.agents/` — out of scope to migrate DevIAC here.

## Awaiting authorization

Authorized 2026-09-20 by the product owner: fix the inconsistencies below, then run Y0→Y4 with a
cold review after every phase. Branch `agentic/dear-tree-tidy`. Y4's accept step stays a named-human act.

## Plan audit (2026-09-20) — inconsistencies found and how they were fixed

| # | Inconsistency | Fix |
| --- | --- | --- |
| 1 | Y0 gate said the privacy watcher is "N/A (private tree)", but the repo is public and `agentic/README.md` on `main` already fails the watcher | Y0 gate now requires the watcher clean on every tracked file Y0 touches; tilde paths become role labels |
| 2 | Criteria table says a pack has `PACK.md`, then names `ide-mcp` (no `PACK.md`) as an example | Y0 adds `agentic/ide-mcp/PACK.md` (new file, not a move) |
| 3 | Master paste said "do not invent SAFE Chicago"; INDEX says the Cathedral cite was fixed 2026-09-20 with `evaluator_safe=yes` | Paste reworded: Chicago allowed only from the DevIAC guide's `evaluator_safe=yes` cite, re-verified live with `ahmes query --cite`; never extrapolated |
| 4 | INDEX promised `arch-001` **and** `arch-014` in the README; Y0 required only `arch-001` | Y0 requires both, each re-read live from `ttod.yml` |
| 5 | Y0's epigraph list was truncated, links unindented | Y0 rewritten |
| 6 | "Fresh agent classifies every entry" (Y2) was a slogan, not a test | Cascade now defines a **legibility test** run at Y0 *and* Y2 by a cold reviewer given only the README |
| 7 | Y1 assumed moves would follow; with the recommended defaults Y2 has zero moves | Y2 may be a **documented no-op**; Y1 decides only after Y0's legibility result |
| 8 | Y4 blurred what an agent may do with proposals | Y4 now bounds the agent to read + disposable-copy dry run; accept needs `--reviewer-id` from a named human |
| 9 | `lao-tzu-tao-compose/` listed as a pack but is untracked with watcher findings | Legend marks it "local, untracked"; not committed by Phase Y |
