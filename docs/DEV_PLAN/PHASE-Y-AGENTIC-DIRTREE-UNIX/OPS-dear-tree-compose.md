# After the Dear Tree tidy — using the compose lens (ops note)

The `lao-tzu-tao-compose` pack is local, **untracked** (verified: `git ls-files` lists nothing under
it), and regenerated by the bibliography pipeline, so this note lives in the tracked plan tree
instead of inside it. (For a few hours it was briefly *tracked* by mistake — a `git add -A`
swept it into an unpushed commit; caught in review, rewound before any push. It has 10
privacy-watcher findings and must not be committed as is.) It records what the lens does
**today**, as measured on 2026-09-20, not what its `SKILL.md` promises.

## Flow

discover → draft → `ttod-bridge` propose (`origin: blackbox`) → **human** `proposal accept
--reviewer-id …` → optional `collections.dear_tree` → `validate --strict` + `stats --check`.
Nothing in the lens writes `ttod.yml`; only the accept step does, by a named human.

## Measured state of each lane (read-only dry run, theme: non-forcing action / wu wei)

| Lane | Command shape | Result 2026-09-20 | Consequence |
| --- | --- | --- | --- |
| Preflight | Ollama tags · gateway `/health` · `INJECT_DONE` marker | all present | lens can run |
| 1. Vectors (DERIVED) | `POST :8100/vectors/search` with `project_slug=scholar-lao-tzu` | **0 results**, with and without `knowledge_scope`, and for seven spelling variants; an unscoped query returns hits only from other slugs. **Root cause (two databases, one Postgres):** the pipeline ingested the bibliography into the **`athanor`** database (1,164 chunks with this slug — verified), but the gateway reads the **`deviac`** database (166,955 chunks, **0** with this slug — verified). The ingest ran and reported success; it wrote to a different database than the one the lane queries | `SKILL.md` step 1 is dead as written |
| 2. Athanor (canonical pointers) | `athanor search "<theme>" --project-slug scholar-lao-tzu --library scholar` | **hits** — e.g. Blakney's translator note on *wei wu wei*, `authority_level: canonical`, page_index 38 | the working discovery lane |
| 3. Ahmes cite (cite-grade) | `ahmes query --cite "$DB:<node>" --style chicago-author-date` | **`[BIBLIO-GAP]`** for the Blakney edition. Cause visible in the data: the node's `document_title` was mis-extracted ("Recommended Reading from MENTOR and SIGNET") and its ISBN is an LLM guess at 0.7 confidence — no operator-asserted bibliography | any Lao-Tzu-grounded proposal must carry **no** Chicago cite. (The Cathedral epigraph in `agentic/README.md` is different: its cite was asserted and reproduces, `evaluator_safe=yes`.) |

`ttod.yml` SHA-256 was identical before and after the run (`9503e81d…`); `git status` on it clean.

## Do not

- Treat lane-1 silence as "no relevant text exists" — it means the query hit a different database from the one the ingest wrote to. Silence and absence are different facts.
- Present a Lao-Tzu-inspired TTOD quote as evidence of anything; TTOD quotes are pedagogical.
- Edit the generated pack from here; fix it at the pipeline (owner action below).

## Frozen harvests — what is and is not empty

Only the two `queries/vectors-*.json` files are empty (`results: []`), for the reason above.
`ttod-voice-samples.json` is not a harvest at all (it records an import error); `graph-tao.json`
holds five vector hits that are not Lao Tzu; the two `athanor-*.json` files hold ten canonical
hits each.

## Owner actions this exposed (not done by Phase Y)

1. Re-run the ingest against the **`deviac`** database (or repoint the pipeline: an Athanor-side
   `DATABASE_URL` leaked into the ingest step), **and** make the pipeline's smoke check assert
   `len(results) > 0` — it currently passes on HTTP status alone while returning `results: []`.
   Otherwise drop lane 1 from the pack's `SKILL.md`.
2. If Lao Tzu grounding should ever be cited, assert bibliographic metadata for the chosen edition
   (as was done for Raymond on 2026-09-20), then re-run lane 3.
3. Sanitize the pack's 10 tilde-path privacy findings before it can be committed, and decide whether a
   pipeline-generated pack belongs in a public repository at all.

## The five staged Dear Tree proposals

In the studio `ttod-bridge` `pending/` directory, stems beginning `20260920T1656`: a dirtree as a
cathedral of names; naming a folder like baptising a star; packs and leaves sharing a parent
without a legend; wu wei in a repository; release early — the layout you can defend. All
`origin: blackbox` (proposed by a model, not yet human-reviewed). Their review is Phase Y4 of the
agentic dirtree tidy ("Phase Y": see this directory's INDEX).
