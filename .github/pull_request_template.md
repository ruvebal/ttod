<!--
Fill this in before requesting review. It mirrors the PR evaluation rubric exactly
(docs/DEV_PLAN/PHASE-V-FEII-COHORT-COLLABORATION-AND-ASSESSMENT.md §5) — there is nothing here
you weren't already going to be graded on; this just makes it checkable per-PR instead of only
at the end.
-->

## What this PR does

<!-- One or two sentences. Link the ASSIGNMENT.md acceptance criterion this satisfies. -->

## Contract adherence

- [ ] Uses the published `src/types/domain.ts` shapes exactly — no parallel/ad-hoc type invented
      for something a shared type already covers

## Correctness & acceptance criteria

- [ ] The specific acceptance line(s) in my module's `ASSIGNMENT.md` this PR satisfies:

## Test coverage

- [ ] Unit/component tests exist for this change (new behavior isn't untested)
- [ ] `npm run check && npm run build` pass locally before requesting review

## Accessibility

- [ ] Keyboard-operable, one accessible name/label, no color-only distinction, respects
      `prefers-reduced-motion` — the Definition of Done from
      `PHASE-V-PRODUCT-BACKLOG-AND-JOURNEYS.md` §3, applies to every PR regardless of module

## AI Review Log (Unit 6 — required, not optional)

<!-- Reuse Unit 6's own template verbatim. A PR missing this entry is incomplete. -->

| What was AI-assisted | What I verified myself |
| --- | --- |
| | |

**AI never approves or merges this PR — only comments. A human approves.**
