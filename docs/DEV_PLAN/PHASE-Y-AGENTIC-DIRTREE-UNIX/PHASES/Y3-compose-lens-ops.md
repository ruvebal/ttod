# Y3 — Compose lens ops (post-tidy)

## Do

1. Verify `lao-tzu-tao-compose/skills/tao-compose/SKILL.md` paths still resolve.
2. Add a short “After Dear Tree tidy” section: discover (`scholar-lao-tzu`) → draft → `ttod-bridge` propose → human accept → optional `collections.dear_tree`.
3. Point at staged proposals from 2026-09-20 (or re-stage if amended).

## Gate

A dry run that **reads** vectors + Athanor for one theme and **does not** write `ttod.yml`.
