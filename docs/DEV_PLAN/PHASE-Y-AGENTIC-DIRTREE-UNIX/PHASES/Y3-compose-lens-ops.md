# Y3 — Compose lens ops (post-tidy)

**Amended 2026-09-20:** the pack is untracked and pipeline-generated, so the "After Dear Tree
tidy" section lives in a tracked note, [`../OPS-dear-tree-compose.md`](../OPS-dear-tree-compose.md),
not inside the pack.

## Do

1. Verify the pack's paths still resolve (Y2 moved nothing, so this is a check, not a fix).
2. Write the flow (discover → draft → propose → human accept → optional collection).
3. **Measure each lane** rather than trusting `SKILL.md`; record dead or blocked lanes honestly.
4. Point at the five staged proposals.

## Gate

A dry run that **reads** vectors + Athanor (+ the cite lane) for one theme and **does not** write
`ttod.yml` — proven by an identical hash before and after.
