# Tao compose rule (Lao Tzu harness)

1. Primary discovery: `project_slug=scholar-lao-tzu`. For **structure/fluidity/
   harness** metaphors, also use the Cathedral companion at
   `~/src/deviac/docs/guides/cathedral-bazaar-companion.md` (vectors
   `scholar-alchemy`) — label the field explicitly; never silently mix coats
   into one cite. Local `queries/cathedral-companion.md` is a pointer only.
2. Never paste a vector `content_preview` into a TTOD quote as if it were a source.
3. Distill pedagogical aphorisms for developers; Lao Tzu informs voice
   (emptiness, naming, soft power, non-forcing); Raymond informs
   cathedral/bazaar structure vs fluid rewrite — not religious doctrine, not
   “ESR proves our cascade.”
4. New quotes go through **ttod-bridge** `propose_quote` with `origin: blackbox`.
5. Prefer `ahmes query --cite` when `evaluator_safe=yes`; keep `[BIBLIO-GAP]`
   honest when not. Raymond coat was operator-asserted SAFE 2026-09-20.
6. Cite existing TTOD entries **by ID** only.
7. Local Ollama only for generative drafting (`localhost:11434`).
8. Harness test when proposing verification wisdom: shared representation =
   artifacts — see `~/src/deviac/docs/guides/studio-harness-test.md` (canonical).
   Cathedral companion: `~/src/deviac/docs/guides/cathedral-bazaar-companion.md`
   (this pack’s `queries/cathedral-companion.md` is a pointer only).
